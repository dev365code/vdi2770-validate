__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.DS_Store
/scratch/
# Fixtures are generated by tools/make_fixtures.py — the generator is the source
# of truth. Editing a built fixture by hand would put the two out of step.
/tests/fixtures/
.coverage
htmlcov/
# The reference implementation's log4j writes application.log into whatever
# directory it is started from. tools/capture_oracle.py now starts it somewhere
# else, and this is the second line of defence — one of these ended up committed.
*.log
